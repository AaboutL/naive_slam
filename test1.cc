#include <iostream>
#include <vector>
#include <stack>
#include <opencv2/opencv.hpp>

int main(){
    cv::Mat img = cv::Mat::ones(100, 100, CV_8UC1);
//    cv::imshow("test", img);
//    cv::waitKey(0);
    return 0;
}